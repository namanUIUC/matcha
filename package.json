{
  "name": "matcha-reco",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Voice-hiring job recommendation engine — ranks jobs against a candidate profile + intake transcript.",
  "scripts": {
    "demo": "tsx --env-file=.env example/demo.ts",
    "compare": "tsx --env-file=.env example/compare.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
